// const appChar = require('../lib/app.characters.js');
// const request = require('supertest');

describe('Rick and Morty Service tests', () => {
  it('makes a request to rick and morty', () => {

  });
});
